# Preserve golden-test bytes identically on every operating system. This also
# covers intentionally malformed .png fixtures that Git might classify as text.
tests/golden/**/*.png -text -diff
tests/golden/reference/**/*.BMP -text -diff
tests/golden/reference/**/*.TIAC -text -diff
tests/golden/reference/**/*.TIAP -text -diff
